
function buildPageInfo() {
    return `
      <form>
      <p id="pInput">If you hypnotize it, it will go away. Let's do it again then, what the heck. It's important to me that you're happy.</p>
      </form>`;
  }

  function buildContactInfo () {
      return `
      `
  }